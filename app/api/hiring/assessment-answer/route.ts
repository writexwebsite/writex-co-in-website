import type {NextRequest}from"next/server";import{z}from"zod";import{apiError,apiOk,notConfigured}from"@/lib/api/response";import{saveAssessmentAnswer}from"@/lib/hiring/assessment-session";import{isHiringFeatureEnabled}from"@/lib/hiring/feature-flags";import{assertRateLimit,assertSameOrigin,getRequestContext,hashValue,parseJson}from"@/lib/security";const schema=z.object({token:z.string().min(24).max(256),questionId:z.uuid(),answer:z.string().trim().min(1).max(30000)});export async function POST(request:NextRequest){try{assertSameOrigin(request);if(!isHiringFeatureEnabled("assessments"))throw notConfigured("Assessment service is unavailable.");const context=getRequestContext(request);assertRateLimit({key:`assessment-save:${hashValue(context.ipAddress)}`,limit:240,windowSeconds:3600});const input=await parseJson(request,schema);return apiOk(await saveAssessmentAnswer(input),{headers:{"cache-control":"no-store"}});}catch(error){return apiError(error)}}

