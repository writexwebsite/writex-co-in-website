import type{NextRequest}from"next/server";import{z}from"zod";import{apiError,apiOk,notConfigured}from"@/lib/api/response";import{submitAssessmentSession}from"@/lib/hiring/assessment-session";import{isHiringFeatureEnabled}from"@/lib/hiring/feature-flags";import{assertRateLimit,assertSameOrigin,getRequestContext,hashValue,parseJson}from"@/lib/security";const schema=z.object({token:z.string().min(24).max(256)});export async function POST(request:NextRequest){try{assertSameOrigin(request);if(!isHiringFeatureEnabled("assessments"))throw notConfigured("Assessment service is unavailable.");const context=getRequestContext(request);assertRateLimit({key:`assessment-submit:${hashValue(context.ipAddress)}`,limit:10,windowSeconds:3600});const{token}=await parseJson(request,schema);return apiOk(await submitAssessmentSession(token),{headers:{"cache-control":"no-store"}});}catch(error){return apiError(error)}}

