import type{NextRequest}from"next/server";import{z}from"zod";import{apiError,apiOk,notConfigured}from"@/lib/api/response";import{isHiringFeatureEnabled}from"@/lib/hiring/feature-flags";import{releaseAssessmentQuestion}from"@/lib/hiring/assessment-session";import{assertRateLimit,assertSameOrigin,getRequestContext,parseJson}from"@/lib/security";
const schema=z.object({token:z.string().min(32).max(300),index:z.number().int().min(0).max(100)});export const runtime="nodejs";export const dynamic="force-dynamic";
export async function POST(request:NextRequest){try{assertSameOrigin(request);if(!isHiringFeatureEnabled("assessments"))throw notConfigured("Assessments are unavailable.");const context=getRequestContext(request);assertRateLimit({key:`assessment-question:${context.ipAddress}`,limit:300,windowSeconds:3600});const input=await parseJson(request,schema);return apiOk(await releaseAssessmentQuestion(input),{headers:{"cache-control":"no-store"}});}catch(error){return apiError(error)}}
